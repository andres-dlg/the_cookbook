import 'package:flutter/material.dart';

class FavouritesList extends StatelessWidget {
  final Color color;

  FavouritesList(this.color);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("FAVOURITE REPCIPES"));
  }
}